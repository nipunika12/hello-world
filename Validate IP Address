def isValid(s):
    #code here
    s=s.split(".")
    if(len(s)!=4):
        return 0
    for i in s:
        if i=="" or i.isalpha() or int(i)>255 or (i[0]=='0' and len(i)>1):
            return 0
       
    return 1
